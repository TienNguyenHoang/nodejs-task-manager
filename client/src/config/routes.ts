const routes = {
    default: '/',
    login: '/login',
    register: '/register',
    editProfile: '/editProfile',
    dashboard: '/dashboard',
    pending: '/pending',
    completed: '/completed',
};

export default routes;
